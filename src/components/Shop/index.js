import React from "react";
import { CShop } from "./style";

const ShopContainer = () => {
  return (
    <CShop>
      <button> SHOP </button>
      <section> </section>
    </CShop>
  );
};

export default ShopContainer;
